{
  "name": "rcomments",
  "version": "1.0.0",
  "description": "Quick and hassle-free traversal of Reddit comments",
  "main": "rComments.js",
  "scripts": {
    "build-js": "cp rComments.js ./dist/rComments.js",
    "build-css": "cleancss rComments.css -o ./dist/rComments.css",
    "build": "mkdir -p dist && yarn run test && yarn run build-js && yarn run build-css && cp manifest.json dist && cp -R icons dist",
    "test": "eslint rComments.js"
  },
  "author": "Ignacio Ampuero",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babili": "0.0.8",
    "clean-css-cli": "^4.3.0",
    "eslint": "^4.18.2",
    "eslint-config-airbnb": "^13.0.0",
    "eslint-config-airbnb-base": "^11.0.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-jsx-a11y": "^2.2.3",
    "eslint-plugin-react": "^6.8.0"
  }
}
